// app/login/page.tsx
export default function LoginPage() {
  return (
    <main style={{ minHeight: "100vh", display: "grid", placeItems: "center", fontFamily: "system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif" }}>
      <div style={{ textAlign: "center", padding: 24 }}>
        <h1 style={{ fontSize: 36, marginBottom: 8 }}>Inloggen (kids)</h1>
        <p>Hier komt straks het inlogscherm (Supabase Auth) en “één sessie tegelijk”.</p>
      </div>
    </main>
  );
}
